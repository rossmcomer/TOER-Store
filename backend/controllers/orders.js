const router = require('express').Router()
const { Product, ProductImage, Category, Supplier, Order, OrderDetail } = require('../models')

router.get('/', async (req, res) => {
})

module.exports = router